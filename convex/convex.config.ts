import { defineApp } from "convex/server";
// TODO: Uncomment when @convex-dev/better-auth is installed
// import betterAuth from "@convex-dev/better-auth/convex.config";

const app = defineApp();
// app.use(betterAuth);

export default app;
